import { PageTemplate } from "@/components/PageTemplate";

export default function Blog() {
    return <PageTemplate title="Blog" subtitle="Insights, updates, and stories from the world of fintech." />;
}
